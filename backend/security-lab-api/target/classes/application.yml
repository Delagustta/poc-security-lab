server:
  port: 8081

spring:
  application:
    name: security-lab-api
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/realm-test-angular

management:
  endpoints:
    web:
      exposure:
        include: health,info
